import java.io.*;
import java.util.Scanner;
public class NodeFun2 {
    public static void main(String [] args) {
        try {
            Scanner sc = new Scanner(new BufferedReader(new FileReader("nums.txt")));
            Node head = new Node(sc.nextInt());
            Node curr = head;
            while(sc.hasNextInt()) {
                curr.setNext(new Node(sc.nextInt()));
                curr = curr.getNext();
            }
            curr = head;
            while(curr != null) {
                System.out.println(curr.get());
                curr = curr.getNext();
            }
        }
        catch(Exception e) {
            System.out.println("An error occured");
        }
    }
}
